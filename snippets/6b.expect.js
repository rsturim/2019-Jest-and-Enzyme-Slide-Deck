test('a description of a given test', () => {
    expect(
        // our code that we're testing
    )
    .toBe(
        // matcher function test
        // the outcome we'd trying to verify
    );
});
